import React from "react";
import {View, Text} from "react-native";

const Tasks = ({value}) =>{
    return (
        <View>
            <Text>{value}</Text>
        </View>
    )
}

export default Tasks