const INITIAL_STATE = {
  fbAuth: null,
  emailAuth: null,
  fbUser: null,
  user: null
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
